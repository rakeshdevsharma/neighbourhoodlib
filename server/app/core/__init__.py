"""Cross-cutting primitives shared by all domain modules.

Configuration, domain enums, input validation, pagination helpers, and
domain exceptions that services raise and the gRPC layer translates to status codes.
"""
